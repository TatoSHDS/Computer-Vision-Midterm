# Computer-Vision-Midterm
Colorize a 3D point cloud model of a garden gnome figure by projecting from the raw geometry (a .xyz file with ~95,000 vertices) and 7 images captured with different cameras and poses, the goal is to estimate a projection matrix for each view and use them collectively to paint realistic, view-consistent color onto every vertex of the 3D model.
